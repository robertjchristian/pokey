# the script uses the python "requests" lib to 
# as an http client.  here's how to do in ubuntu:
sudo apt-get install python libevent-dev python-pip python-dev build-essential 
sudo pip install --upgrade pip 
sudo pip install --upgrade virtualenv 
sudo pip install gevent
sudo pip install requests
